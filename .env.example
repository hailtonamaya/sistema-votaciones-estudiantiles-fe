# Copia este archivo a `.env` (mismo directorio raíz del proyecto)
# y completa los valores reales. NUNCA subas el .env a git.

# URL base de la API del backend (incluye /api).
# En desarrollo local con `npm run dev` del backend, déjalo así:
VITE_API_URL=http://localhost:3000/api
